import type React from "react"
import type { Metadata } from "next"
import { Poppins } from "next/font/google"
import "./globals.css"

const poppins = Poppins({
  weight: ["300", "400", "500", "600", "700"],
  subsets: ["latin"],
})

export const metadata: Metadata = {
  title: "Luminara: A Mental Health & Wellness Hub",
  description:
    "Luminara is a Suicide Lifeline Initiative, a non-profit unit, committed to suicide prevention and mental health advocacy.",
  keywords: "suicide prevention, crisis support, mental health, counseling, support groups",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={poppins.className}>{children}</body>
    </html>
  )
}
